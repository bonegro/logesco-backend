package com.schools.logesco.impression.repository;

import com.schools.logesco.impression.entity.ListePersonnalisee;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ListePersonnaliseeRepository extends JpaRepository<ListePersonnalisee, Long> {
}
